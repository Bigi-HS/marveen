{
  "id": "developer-senior",
  "label": "Fejlesztő (senior, bizalmi)",
  "description": "Bizalmi profil: permissive módban fut, minden parancs engedélyezve, kivéve a deny-listát (sudo, SSH/AWS/GnuPG kulcsok olvasása, force-push, main-push, rm -rf $HOME, pkill, access.json hamisítás). Csak olyan ágensnek add, amit ugyanannyira megbízol mint magadat. Nem sandbox. Sandboxolt profilért lásd developer-junior.",
  "permissionMode": "permissive",
  "filesystem": {
    "allow": [],
    "deny": [
      "Read(${HOME}/.ssh/**)",
      "Read(${HOME}/.aws/**)",
      "Read(${HOME}/.gnupg/**)",
      "Bash(sudo:*)",
      "Bash(rm -rf ${HOME}:*)",
      "Bash(rm -rf /:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(git push origin main:*)",
      "Bash(git push origin master:*)",
      "Bash(pkill:*)",
      "Bash(killall:*)",
      "Write(**/access.json)",
      "Edit(**/access.json)"
    ]
  }
}
