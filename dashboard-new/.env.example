# Copy to .env.local for local dev. The dashboard reads the backend bearer token
# at build/dev time (AC-G5). Never commit a real token. For local dev you can
# instead set localStorage["noa-api-token"] in the browser and leave this unset.
VITE_DASHBOARD_TOKEN=
